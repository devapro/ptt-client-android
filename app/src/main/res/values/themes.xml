<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the launch window. The app draws edge-to-edge Compose from the first frame; this
         theme exists so the cold-start window matches the palette instead of flashing white. -->
    <style name="Theme.PTTdroid" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
