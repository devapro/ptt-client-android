package com.github.devapro.pttdroid.data.settings

/**
 * No-op placeholder (same shape as Phase 7a's iOS audio stubs). The language selector still
 * persists through [SettingsRepository]; applying it to UIKit/Compose locale is a later phase.
 */
actual fun applyLanguagePreference(mode: LanguageMode) {
}
